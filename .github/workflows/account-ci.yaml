name: account service ci

on:
  push:
    branches: [ "main" ]
    paths:
      - "account/**"
      - "base/**"
      - ".github/workflows/common/action.yaml"
      - ".github/workflows/account-ci.yaml"
      - "pom.xml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "account/**"
      - "base/**"
      - ".github/workflows/common/action.yaml"
      - ".github/workflows/account-ci.yaml"
      - "pom.xml"
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    env:
      FROM_ORIGINAL_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name == github.repository || github.ref == 'refs/heads/main' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis
      - uses: ./.github/workflows/common
      - name: Run Maven Checkstyle
        run: mvn spotless:check -f account -q
      - name: Run Maven Verify
        run: mvn clean verify -pl account -am -q
      - name: Add test report to PR
        uses: turing85/publish-report@v2
        if: ${{ always() }}
        with:
          report-name: 'Account Test'
          report-path: "account/target/surefire-reports/TEST*.xml"
          report-fail-on-error: 'true'
          report-reporter: 'java-junit'
          comment-message-recreate: |
            <h3>🔄 Account Test Report for run [#${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})</h3>
            <blockquote>Result will be posted here as they get available.</blockquote>
          comment-message-success: |
            <h3>🥳 Test passed</h3>
            
            | Passed | Failed | Skipped |
            | ------ | ------ | ------- |
            | ✅ {1} | ❌ {2} | ⚠️ {3} |
            
            You can see the full report [here]({4})
          comment-message-failure: |
            <h3>😢 Test failed</h3>
            
            | Passed | Failed | Skipped |
            | ------ | ------ | ------- |
            | ✅ {1} | ❌ {2} | ⚠️ {3} |
            
            You can see the full report [here]({4})
#      - name: Analyze with sonar cloud
#        if: ${{ env.FROM_ORIGINAL_REPOSITORY == 'true' }}
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#        run: mvn org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -f account
      - name: OWASP Dependency Check
        uses: dependency-check/Dependency-Check_Action@main
        env:
          JAVA_HOME: /opt/jdk
        with:
          project: 'jhcm'
          path: '.'
          format: 'HTML'
      - name: Upload OWASP Dependency Check results
        uses: actions/upload-artifact@v4
        with:
          name: Account OWASP Dependency Check Report
          path: ${{github.workspace}}/reports
      - name: Add coverage report to PR
        uses: madrapps/jacoco-report@v1.6.1
        if: ${{ env.FROM_ORIGINAL_REPOSITORY == 'true' }}
        with:
          paths: ${{github.workspace}}/account/target/site/jacoco/jacoco.xml
          token: ${{secrets.GITHUB_TOKEN}}
          min-coverage-overall: 80
          min-coverage-changed-files: 70
          title: 'Account Coverage Report'
          update-comment: true
      - name: Log in to the Container registry
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push Docker images
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: docker/build-push-action@v6
        with:
          context: ./account
          push: true
          tags: ghcr.io/vnworkday/jhcm-account:latest